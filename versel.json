{
    "builds": [{
        "src": "organizer_fine_api/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "organizer_fine_api/wsgi.py"
        }
    ]
}